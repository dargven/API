env: "local"
storage_path: "./Storage/storage.db"
http_server:
  address: "localhost:8082" #у нас скорее всего надо ставить 8080, у нас ведь ничего не запущено на стандартом порту
  # Похуй че ставить
  timeout: 4s
  idle_timeout: 30s
  user: "my_user"
  password: "my_pass"
data_base:
  host: localhost
  port: 5433
  user: dargven
  password: root
  name: api